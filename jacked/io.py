# place holder for downloading the data with a simple python command. 
# Note this needs to be done in Python 3.6

import os
import requests
import zipfile

def fetch(folder_name,
          url = "https://github.com/Joshiwavm/ms_file",  
          extract_to='/tmp/'):
    ...